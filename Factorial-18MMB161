N = input('To find the factorial, enter a whole number');
if (isreal(N) && rem(N,1)==0);
    if N>0
        disp('N=');
        disp(N);
        f = factorial(N);
    disp('N!=');
    disp(f);
    elseif N==0
        Disp('N=');
        Disp(N);
        disp('N!=1');
    elseif N<0
        disp('Domain Error! Input must be a whole number');
    end
else disp('Domain Error! Input must be a whole number');
end
























